/**
 * Apartment photos shown under the hero. Drop files in public/photos/ and list them here.
 * Order matters; the first one is shown largest.
 */
export const photos: { src: string; alt: string }[] = [];
